{
  "name": "store",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && aws s3 sync dist/ s3://$S3_BUCKET --delete --cache-control 'max-age=31536000,immutable' && aws s3 cp dist/index.html s3://$S3_BUCKET/index.html --cache-control 'no-cache, no-store, must-revalidate'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
